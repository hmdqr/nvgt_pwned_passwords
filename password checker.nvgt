// Test script for Pwned Passwords Checker
#include "pwned_passwords.nvgt"

void main() {
    
    string user_password = input_box("Check Password", "Enter a password to check:");
    if (user_password.is_empty()) return;
    
    string result = check_pwned_password_message(user_password);
    alert("Result", result);
}
